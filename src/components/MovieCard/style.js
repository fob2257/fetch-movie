import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  cardContainer: { marginBottom: 8 },
  poster: { width: 120 },
  titleGridContainer: { paddingLeft: 8 }
}));

export default useStyles;
